"""Runnable examples built on the :mod:`libkp` public API.

- :mod:`libkp.examples.meters` — a live full-screen terminal view of the current
  patch, effect blocks, tuner strobe, and level meters.

Run one with ``python -m libkp.examples.meters --help``.
"""
